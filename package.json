{
  "name": "produdash",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Secure local operations workspace for connected commerce and approval-gated AI workflows.",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "electron .",
    "app": "electron .",
    "web": "python3 -m http.server 5188",
    "check:syntax": "node scripts/check-syntax.cjs",
    "lint": "eslint electron src test smoke scripts",
    "format": "prettier --write electron src test smoke scripts .github package.json README.md index.html eslint.config.mjs .prettierrc.json",
    "format:check": "prettier --check electron src test smoke scripts .github package.json README.md index.html eslint.config.mjs .prettierrc.json",
    "test": "node --test test/*.test.cjs",
    "test:smoke": "node --test smoke/*.test.cjs",
    "validate": "npm run check:syntax && npm run lint && npm run format:check && npm test && npm run test:smoke"
  },
  "keywords": [
    "dashboard",
    "shopify",
    "social-media",
    "ai-assistant"
  ],
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "@google/genai": "^2.13.0"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "eslint": "^10.7.0",
    "jsdom": "^29.1.1",
    "playwright": "^1.61.1",
    "prettier": "^3.9.6"
  }
}
